/**
 * For vuetify light, dark themes
 */
export default {
    props: {
        light: {
            type: Boolean,
            default: false,
        },
        dark: {
            type: Boolean,
            default: false,
        },
    }
}