global.hitama = {
    testmodes: {
        testdata: false, // make api_cmd return fake response data for offline testing without project build
        logintest: true // load testdata for login/register-Modals
    }
};
